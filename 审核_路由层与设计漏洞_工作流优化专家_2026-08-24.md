# HealthPick「AI 智能膳食顾问」设计漏洞审核报告
**审核角色**：工作流优化专家（WorkflowOptimizationExpert）
**审核对象**：`10_项目源码_healthpick`（backend + frontend + deploy + knowledge）
**基准**：`OPC接单吧第二届实战能力大赛（软件与智能体赛道大赛试题）.docx`（赛题需求）
**审核日期**：2026-08-24

---

## 0. 审查边界声明（先钉死，避免踩坑）

团长明确：**历史测试数据 / 旧文档不可信，不能看**。本报告严格遵守：

- ✅ **只看两类真相**：赛题需求文档（.docx）+ 项目源码（.py / .Dockerfile / .yml / README）。
- ❌ **未读取**：`30_测试记录/`、`20_基线能力地图/`、`pytest_*.log`、`resp1.json`、各 `*_压测_*.md` 等历史测试产物。
- 所有结论均给出**源码文件:行号** 或 **需求条款** 作为依据，可逐条复核，不凭历史记录下判断。

---

## 1. 一句话总评

> 赛题**基础需求已达标**（代码层面能对照需求逐条落地），但"达标"靠的是一层**巨型 if/elif 路由函数 + 关键词补丁墙**堆出来的；作为"真正的产品"，它在**路由层架构、生产态状态管理、鉴权、检索技术**四个地方有结构性选型错误，不重构会被并发、被越权、被语义召回漏判三件事同时击穿。

---

## 2. 达标情况速览（对照需求，确认底线已过）

| 基础需求 | 代码落地位置 | 结论 |
|---|---|---|
| 1.1 知识库 A/B/C 分类挂载 | `retrieval.py:4-9`（A+B 同域、C 隔离）、`main.py:2236-2250`（C 块排除重选） | ✅ 达标 |
| 1.2 回答准确性 + 1.3 引用溯源 | `main.py:2234` `_pick_reply_chunk` + `sources` 回传 | ✅ 达标 |
| 2 多轮对话与上下文记忆 | `main.py:1902` `_recent_user_messages` + `models.Message` 落库 | ✅ 达标（但浅，见 L6） |
| 3 方案/食材推荐 + 禁忌排除 | `_plan_from_goal` / `_build_meal` + `compliance.excluded_foods` | ✅ 达标 |
| 4 可运行系统 + UX + 文档 | `README.md` 一键启动、`frontend/index.html`、`deploy/` | ✅ 达标 |
| 健康免责声明 | `compliance.DISCLAIMER_STANDARD` 强制注入（`main.py:1938`） | ✅ 达标 |

> 注意：上表是**代码能力**对照，**不是测试结果**。团长要求不引用历史测试数据，故此处不引用任何用例数。

---

## 3.【在产技术选型错误】T1–T6

### T1 状态管理选型：进程内存 + JSON 文件当"分布式状态"（生产不可扩展）
- **现象**：成本闸门的限速率 `self._rate` 是**进程内 dict**（`cost_gate.py:58`）；预算账本与回复缓存是**磁盘 JSON 文件**（`cost_gate.py:32-36`）。
- **部署现实**：`Dockerfile:26` 是单 worker `uvicorn app.main:app`；`docker-compose.yml` 也未起多实例。
- **错误**：当前"能跑"只是因为只有一个进程。一旦为生产并发起 **≥2 个 worker**（或水平扩容），各进程账本/缓存/限速**各自为政** → 预算上限与限速形同虚设，JSON 缓存被并发重写还会争锁。这是用**本地玩具态**冒充**生产状态层**。
- **正确选型**：预算/限速/缓存应外置到 Redis（或 DB 原子计数），与进程数解耦。

### T2 数据库选型：SQLite 默认 + 无迁移（并发写会锁/丢）
- **现象**：`config.py:77-79` `DATABASE_URL` 默认 `sqlite:///...healthpick.db`；`docker-compose.yml:18` 也是 SQLite 卷；`models.py:36` 靠"幂等 ALTER 补列"升级，无 Alembic 迁移。
- **错误**：SQLite 单写锁，多用户同时写 `diet_logs / messages / sessions / profile` 会串行化甚至写失败。健康产品要存用户 PII + 健康数据，SQLite 是**演示级**选型，不是生产级。
- **正确选型**：Postgres +  migrations（Alembic），至少为多写场景准备。

### T3 鉴权选型：零认证授权，user_id 由客户端自报（健康数据裸奔）
- **现象**：`models.User` 只有 `id/nickname/profile_json`，**无任何凭证字段**（`models.py:30-44`）；限流键来自客户端头 `X-Session-Id / X-User-Id`（`guard.py:117-129`），非认证身份；`/api/history?user_id=...`（`main.py:2568-2572`）凭一个字符串返回**任意用户**全量对话/过敏/档案。
- **错误**：任何人猜到别人 `user_id` 就能读其健康档案与对话历史。对存健康数据的产品，这是**安全硬伤**，不是进阶项。
- **正确选型**：至少加轻量签名 session token（网关 JWT / 签名 cookie），`/api/history` 必须校验"只能查自己"。

### T4 LLM 调用选型：同步阻塞 + 无流式 + 单供应商（UX 与韧性双弱）
- **现象**：`llm.synthesize` 是**同步** `httpx.post(timeout=15)`（`llm.py:63-96, 211`）；`chat()` 是 `def` 非 `async def`（`main.py:1894`）；全仓**无 `StreamingResponse`**；只有 DeepSeek 一家，无 fallback。
- **错误**：用户面对一个最长 15 秒的转圈；单供应商故障即全量降级；同步调用占满 worker 线程，吞吐上不去。现代对话产品的基本盘是流式 + 异步 + 多供应商容灾。
- **正确选型**：`async` + SSE/流式 + provider failover（DeepSeek/通义/本地模型兜底）。

### T5 检索技术选型：BM25 + jieba 词面匹配作为唯一 RAG（语义召回天花板）
- **现象**：`retrieval.py:1-10` 显式"不调用任何向量库/embedding 服务"，只用 `rank_bm25` + `jieba`（`retrieval.py:105-120`）。
- **现实代价**：用户 paraphrase（"我想掉秤"/"减肚子"/"怎么吃能瘦"）能否命中，完全取决于 `synonyms.json` + `_DIETARY_HINTS` + `_PLATFORM_HINTS` 是否枚举到。注释自己承认过 **C 库因词表漂移"形同虚设"**（`retrieval.py:25-27`）。
- **判断**：48 小时赛题 + 离线零依赖下这是**可辩护的取舍**；但作为"真正的产品"，纯词面 RAG 是**已知召回天花板**，会被同义/口语表达漏判。
- **正确选型**：BM25 + 本地向量（bge-small-zh 等）**混合召回 + rerank**，离线也能跑小模型 embedding。

### T6 审计完整性选型：HMAC 默认关闭
- **现象**：`config.py:92-94` `AUDIT_HMAC_KEY` 默认空 → 审计账本降级为"仅哈希链，无签名"，仅记 warning。
- **错误**：健康产品审计日志可被篡改且无签名校验，合规场景站不住。
- **正确选型**：prod 强制启用 HMAC 签名（缺失即启动失败，而非降级）。

---

## 4.【逻辑选型错误】L1–L8（L1 是团长点名的"路由层大问题"）

### L1 路由层架构：约 570 行的巨型 if/elif 分派函数（核心病灶）
- **现象**：`main.py:1894-2464` 的 `chat()` 把**整个意图路由**塞进一个函数：拒药 → 数值查表 → 一餐 → 点单决策 → P5 → 危险信号 → 辟谣 → 人设 → 平台 → 疾病 → 闲聊 → BM25 → LLM，全靠嵌套 `elif` 顺序分支。
- **补丁驱动的铁证**（均在源码注释，非测试数据）：`R1/R2 修复`、`暴风雪回归`、`终极压测`、`三审修复`、`Gemini 审查回归`、`第七波`、`二审 P0/P1`、`V01–V12`…… 几乎每一段分支都挂着"为某个测试场景打的补丁"。
- **错误**：路由本应是"分类器 → handler"的**可扩展管线**，结果变成了**关键词打地鼠**。加一个新意图 = 改 570 行函数；分支优先级一旦错位，bug 级联（注释显示已多次级联）。这正是团长感受到的"路由层出大问题"的根。
- **正确逻辑**：抽 `IntentRouter`（意图分类 → handler 注册表 → 显式优先级管线），每个意图一个独立 handler + 单测。

### L2 意图优先级靠"分支书写顺序"隐式约定，无单一真相
- **现象**：铁律写"用药硬拦截 → 数值 → 一餐 → 检索/LLM"（`README.md:50`），但代码里这优先级是靠 `elif` 顺序 + 到处补 `not is_med_query` 守卫实现的（`main.py:1982-2098` 多处）。
- **错误**：同一优先级被复制进十几个分支，回归风险高。应有一个**有序管线 + 早退**，而非散落守卫。

### L3 关键词表重复定义且已漂移（单一真相被违反）
- **现象**：`retrieval.PLATFORM_HINTS`（`retrieval.py:28-39`）与 `main._PLATFORM_HINTS`（注释明说"此前两处漂移，C 素材形同虚设"，`retrieval.py:25-27`）是两份平台词表；疾病/用药词在 `compliance.py` 与 `main.py` 也分头维护。
- **错误**：意图词典散落多文件、手动同步，必然再漂移。应集中为**单一意图词典**。

### L4 缓存语义：单轮 + 按用户隔离，跨用户事实不缓存（钱白白烧）
- **现象**：`llm.py:147-163` 回复缓存仅 `history is None` 时启用，且 key 含 `user_id`。
- **错误**："鸡胸肉多少千卡"这类**事实性问题**被成千上万用户重复问，却因 key 含 user_id **不跨用户命中** → 每次都烧 API。为防"跨会话串扰"这一个 bug，把最该缓存的事实查询也放弃了。
- **正确逻辑**：context-free 的事实/方案问答用**全局语义缓存**（按归一化 query 命中），只有带用户上下文的多轮才跳过。

### L5 产品定位逻辑：把"顾问/陪伴"做成"查表 + 问答工具"，缺真正闭环
- **现象**：所有 handler 是 `medication_refuse / numeric_lookup / meal / decision / allergy_block / chitchat / platform / disease / nutrition_qa`——本质是**查表 + 模板填充 + 拒答**。"陪伴"只是 system prompt 人设 + streak 文案（`health_state.py:120-129`），没有"记录 → 反思 → 调整方案"的闭环。
- **错误**：需求业务背景要求"健康管理引导"（`赛题:18`），但架构为**过合规红线**优化、不为**用户真实行为改变**优化。作为"真正的产品"，这是定位层面的逻辑错配——它是个 Nutrition QA 工具，不是膳食顾问。
- **正确逻辑**：`profile → plan → log → reflect → adapt` 闭环：用 `diet_logs` + `health_state` 驱动每周回顾与方案微调，而不只是记 streak。

### L6 多轮记忆逻辑：只传最近 4 句原文，无结构化用户态
- **现象**：`main.py:1902` 取最近 2 句拼 `ctx_query`；`_recent_chat_turns(...,4)` 传 4 句原文；`health_state` 只算 streak/日报（`health_state.py:95-117`）。
- **错误**：一个"该记住你情况"的顾问，上下文只有 4 句裸文本 + 一个连续天数。无用户状态结构化注入检索/生成。
- **正确逻辑**：维护结构化 user-state（目标/禁忌/口味/进度），注入检索与生成，而非末 N 句字符串。

### L8 安全相关失败被静默吞掉（降级不崩 ≠ 安全）
- **现象**：过敏画像、用户档案写入均为 `except: 非致命`（`main.py:2382-2439`、`_persist_turn:1859-1891`、`user_profile.py:124-127`）。
- **错误**：DB 瞬时故障会**静默丢掉本回合检测到的过敏**，下一轮记忆里没了 → 禁忌排除断链。可用性哲学对安全关键路径有害。
- **正确逻辑**：过敏/禁忌这类安全态的持久化失败应**可观测 + 重试**，至少告警，而非悄悄跳过。

---

## 5. 正向资产（必须保留，别误改）

这些是**正确**的设计，优化时不能动：
- **数值确定性计算**（`nutrition_lookup` + `main.py:2109-2145`）：红线⑤"数值必须工具算、禁模型编造"——对医疗数值这是**对的安全选择**，保留。
- **合规层确定性规则**（`compliance.py` 拒药/免责/禁忌）：安全红线用硬规则而非模型自觉，正确。
- **A/B/C 知识隔离**（`retrieval.py:4-9`）：防素材 C 价格混入营养建议，正确且满足赛题素材规范。
- **优雅降级**（无 Key 走 local-rules，`llm.py:133-139`）：可用性哲学正确。
- **入口守卫 + 密钥脱敏**（`guard.py` C0 控制符剥离、`secret_redaction.py`）：安全卫生正确。

---

## 6. 把它变成"真正的产品"：升级路线

### P0（先止血，再做产品化）
1. **重构路由层**（根治 L1/L2/L3）：拆 `chat()` → `IntentRouter`（意图分类 + handler 注册表 + 显式优先级管线 + 单一意图词典）。这是团长点名问题的根。
2. **加鉴权**（T3）：轻量签名 session token，`/api/history` 只能查自己。
3. **状态外置**（T1/T2）：cost gate 限速/预算/缓存 → Redis；SQLite → Postgres + Alembic。多 worker 才安全。

### P1（质量与成本）
4. **检索升级**（T5）：BM25 + 本地向量混合 + rerank，补语义召回。
5. **LLM 异步流式 + 多供应商容灾**（T4）。
6. **全局事实缓存**（L4）：context-free 问答跨用户命中，降本。
7. **审计 HMAC 强制开启**（T6）。

### P2（产品闭环）
8. **闭环逻辑**（L5/L6）：`profile→plan→log→reflect→adapt`，每周回顾驱动方案微调；结构化 user-state 注入检索与生成。
9. **真正陪伴**：从"streak 文案"升级为"基于记录的行为改变引导"。

---

## 7. 依据索引（逐条可复核）

| 编号 | 源码依据 | 需求/原则依据 |
|---|---|---|
| T1 | `cost_gate.py:58,32-36`；`Dockerfile:26` | 生产可扩展性 |
| T2 | `config.py:77-79`；`docker-compose.yml:18`；`models.py:36` | 生产数据层 |
| T3 | `models.py:30-44`；`guard.py:117-129`；`main.py:2568-2572` | 健康数据安全 |
| T4 | `llm.py:63-96,211`；`main.py:1894` | UX/韧性 |
| T5 | `retrieval.py:1-10,105-120,25-27` | 回答准确性（赛题 1.2） |
| T6 | `config.py:92-94` | 审计合规 |
| L1 | `main.py:1894-2464`（含 R1/R2/暴风雪/终极压测/三审 等补丁注释） | 路由可扩展性 |
| L2 | `main.py:1982-2098`；`README.md:50` | 意图优先级单一真相 |
| L3 | `retrieval.py:25-39` | 单一意图词典 |
| L4 | `llm.py:147-163` | 成本优化 |
| L5 | `main.py` 各 handler；`health_state.py:120-129`；赛题 `:18` | 健康管理引导 |
| L6 | `main.py:1902`；`health_state.py:95-117` | 上下文记忆（赛题 2） |
| L8 | `main.py:2382-2439,1859-1891`；`user_profile.py:124-127` | 安全态持久化 |

---

*本报告仅依据需求文档与源码得出，未引用任何历史测试数据。如需我针对 P0 第 1 项（路由层重构）产出可直接落地的 `IntentRouter` 设计稿，或针对 T3 产出鉴权方案，告诉我即可。*
