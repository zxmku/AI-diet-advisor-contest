# AI 智能膳食顾问（HealthPick）· v1.20 最终版本

> ## ⚠️ 本仓库为最终交付版本
>
> 提交表单中的初版因**误以为赛题按"48 小时"截止**而先行提交，后发现平台实际截止为 **8 月 26 日**，故在窗口期内持续迭代完善。**请以本仓库（GitHub）为最终版本评审**，全部能力、文档与测试均以本仓库为准。
>
> 版本说明见《版本说明.md》｜ 本地部署见《本地部署手册.md》｜ 使用说明见 `20_交付文档/使用说明.md`

## 🌐 在线体验（推荐）

> 在线演示链接（部署后填写）：
> **https://＿＿＿＿＿＿＿＿**
>
> 点击即用完整 AI 膳食顾问（已配置密钥，无需任何本地操作）。

## 快速开始（本地）

1. 确保已装 **Python 3.10 / 3.11 / 3.12**（离线依赖包仅内置这三版）。
2. 双击根目录 **`一键启动.bat`** → 离线安装自带依赖（无需联网）→ 浏览器自动打开 `http://localhost:8137`。
3. 开启完整 AI 模式（推荐）：用记事本打开 `deploy/.env`，填 `DEEPSEEK_API_KEY=你的密钥`，保存后重新双击即可。

> 完整部署步骤（命令行 / Docker / 密钥配置 / 故障排查）见 **《本地部署手册.md》**。

## 产品是什么

面向「减脂塑形 / 增肌强化 / 慢病调理」人群的 **AI 膳食陪伴顾问**：不止查热量、推方案，更能**搭出完整一餐**（食材 + 克数 + 热量 + 做法 + 替换）、**记住你的目标与过敏**、**在你加班嘴馋时先共情再给无痛替代**——聊起来、记得我、陪你坚持。

- **知识库问答**：素材 A 营养基础 / B 三套方案 / C 平台服务（物理隔离），回答带来源标注、数值 100% 查表不编造
- **多轮对话与记忆**：会话上下文 + 目标/过敏画像跨轮延续 + 饮食台账与坚持天数
- **方案与一餐**：减脂 / 增肌 / 控糖三套方案 + 一餐生成（速查表 × 份量计算）
- **红线合规**：拒药硬拦截 / 禁忌全链路排除 / 医疗免责 / C 库隔离 / AI 辅助标注如实
- **双模式**：配置密钥 = LLM 产品级；未配置 = 本地规则兜底（功能完整，仅无 AI 润色）

## 目录分区

| 分区 | 职责 |
|---|---|
| `01_需求拆解与业务还原/` | 赛题理解（需求拆解） |
| `02_业务SOP与流程限制/` | 业务红线守门员审查清单 |
| `03_架构蓝图与系统方向/` | 系统架构蓝图（v3） |
| `04_交付文档与测试报告/` | 交付物索引 |
| `20_交付文档/` | 使用说明 / 架构说明 / AI辅助标注 / 基础功能测试记录 |
| `20_基线能力地图/` | 能力地图 + 三大支柱方案 |
| `30_测试记录/` | 红线回归 / 暴风雪 / 压测 / 真人视角产品级测试 |
| `backend/` | FastAPI 后端（对话编排 / 合规层 / 检索 / 一餐生成 / 台账） |
| `frontend/` | 单页聊天界面（零构建、零依赖） |
| `knowledge/` | 素材 A/B/C 结构化知识库 + 禁忌映射 + 同义词 |
| `tests/` | pytest 全量测试（**258 用例全绿**） |
| `deploy/` | 离线依赖包（35 wheels）/ Docker / .env.example / 启动脚本 |
| `产品定位/` | 产品定位与设计方向 |
| `assets/` | 图标与启动入口资源 |

## 质量门禁

自动化测试：`python -m pytest tests -q`（**258 passed**）。含红线回归、100 条攻击式指令压力测试（暴风雪）、外部审查与用户视角刁难压测，缺陷闭环并回归锁死；详见 `30_测试记录/`。

## 技术要点

- FastAPI 单端口托管前后端（8137），前端零构建
- BM25 检索（rank_bm25 + jieba + 同义词扩展），A/B 库合并索引、C 库独立隔离
- LLM 合成层（DeepSeek）：检索 grounding + 用户画像注入 + 成本闸门 / 限速 / 缓存
- 双引擎容灾：未配置密钥或模型失败 → 自动本地规则兜底，绝不白屏
- 部署：Docker（`deploy/docker-compose.yml`）或 Windows 一键启动（离线 wheels）
