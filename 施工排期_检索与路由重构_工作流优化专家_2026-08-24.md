# 施工排期 · 检索精准度提升 + 路由层重构（合并）

> 编制：工作流优化专家（人 B）
> 日期：2026-08-24
> 范围：HealthPick 项目「从达标到真产品」结构性改造
> 约束：只认赛题需求 `.docx` + 项目源码为真相来源；历史测试数据/基线地图/pytest 日志一律不采信（团长死命令）

---

## 一、两条主线定位

| 主线 | 目标 | 当前病灶（源码证据） | 风险 | 收益 |
|---|---|---|---|---|
| **A 混合检索改造** | 提升长尾/近义问法命中率 | `retrieval.py` 仅 BM25+jieba 关键词匹配，无向量语义召回；膳食指南/方案/白皮书为专业语料，近义表述会漏判 | **低**（独立模块，不动路由/LLM） | **高**（精准度直接提升，引用溯源不破） |
| **B 路由层重构** | 消除「补丁墙」，主干稳定可扩展 | `main.py` 的 `chat()` 是 1000+ 行 if/elif 巨型分派，靠关键词硬匹配逐层分支；注释满是 `# R1/R2 修复 / 暴风雪回归 / 三审修复`，属测试堆出来的非设计态 | **中**（影响面大，需全量回归） | **高**（扩展只动 handler/配置，主干不再脆弱） |

**关键判断**：A 是「填坑提效」，B 是「换地基」。A 低风险先上快速见效；B 是团长点名的「出大问题」主线，不能无限期拖，但需先设计评审再动。

---

## 二、依赖关系

```
阶段0  达标基线冻结（已完成，见审核报告）
  │
  ├──────────────┐
  ▼              ▼
阶段1          阶段2
混合检索改造    路由层分层化
（独立模块）    （主干重构，需设计评审）
  │              │
  └──────┬───────┘
         ▼
      阶段3  集成 + 回归（检索作为知识库 handler 底层；cost_gate/compliance 不破）
         │
         ▼
      阶段4  图谱(GraphRAG) 触发式（远期，可选）
```

- **A 不阻塞 B**：检索是独立模块，可先单独交付。
- **B 不阻塞 A**：路由重构前检索层已可被现有 if/elif 分支调用。
- **阶段3 依赖两者**：集成时把「混合检索」塞进「知识库问答 handler」底层。
- **阶段4 触发式**：满足条件才启动，不为「轻量」目标提前引入复杂度。

---

## 三、阶段排期

### 阶段 0 · 达标基线冻结（已完成）
- 产出：前序审核报告 `审核_路由层与设计漏洞_工作流优化专家_2026-08-24.md`
- 作用：锁死赛题基础需求为不可动摇底线，后续改造不得跌破。

### 阶段 1 · 混合检索改造（低风险，先行 · 建议 1–2 天量）
**改动文件**
- `retrieval.py`（改造：BM25 保留 + 向量召回 + RRF 融合）
- 新增 `build_index.py`（建库脚本，可重跑）
- `requirements.txt` 增 `chromadb` + `fastembed`

**做法**
1. 保留现有 BM25+jieba（零风险基线）。
2. 向量层用 **Chroma 嵌入式**（无 server，本地持久化，与 SQLite 同机）+ **FastEmbed**（`bge-small-zh` 本地 ONNX，无 API key）。
3. 检索时两路并行：BM25 topN + 向量 topN → **RRF（Reciprocal Rank Fusion）** 融合排序。
4. 建库脚本灌素材 A/B/C 三份 PDF，生成 BM25 索引 + Chroma 向量库。

**验收**
- 长尾/近义问法命中率提升（用新构造的问法集验证，不依赖历史数据）。
- 引用溯源（chunk 出处）不破坏。
- 建库脚本可重跑、可增量。

**风险**：低。若向量层异常，BM25 单路兜底仍可用。

### 阶段 2 · 路由层分层化（核心，需设计评审 · 建议 3–5 天量）
**改动文件**
- `main.py` 的 `chat()` 拆出 → 新增 `intent.py`（意图识别层）+ `handlers/` 目录（领域处理器 A/B/C）+ 保留 `compliance.py`/`cost_gate.py` 挂钩点。

**做法（分层意图路由）**
1. **意图识别层**：轻量分类（关键词白名单 + 必要时 LLM 判定），输出 `intent` 标签。
2. **领域处理器**：每个意图一个 handler（知识库问答 / 方案推荐 / 多轮记忆 / 兜底），handler 内调用 `retrieval`（阶段1 已升级）、`decision_tool`、`compliance`、`cost_gate`。
3. **兜底 + 人工介入**：低置信/未知意图走兜底话术，必要时标记人工介入。

**验收**
- 新增意图只改 `handlers/` 或配置，主干 `chat()` 不再膨胀。
- 测试可局部化（每个 handler 独立用例），不再「改一个意图重跑全套」。
- `compliance`（免责声明/红线条）/ `cost_gate`（成本闸门）在每条链路均被调用，不破。

**风险**：中。需先出「意图清单 + handler 映射表」设计评审，再落地；全量回归以新用例为准。

### 阶段 3 · 集成 + 回归（依赖 1&2 · 建议 1–2 天量）
- 把阶段1 混合检索作为「知识库问答 handler」底层实现。
- 全量回归：以**新构造的验收用例集**为准（不读历史测试数据），重点验 cost_gate 不被击穿、compliance 红线条不漏。
- 部署复核：单 worker / 无 Redis 现状下，Chroma 嵌入式 + 本地 FastEmbed 是否在并发下安全（必要时加文件锁或进程内单例）。

### 阶段 4 · 图谱（GraphRAG）触发式（远期，可选）
- **触发条件**：要做「食材禁忌多跳推理」（糖尿病→忌高糖→某食材含糖 X→替换建议）这类强关系推理时才上。
- **现在不上**：当前问答主要是片段召回+引用，BM25+向量足够；图谱需 LLM 抽实体+高质量 schema，过早引入 = 违背「轻量」目标。
- **借鉴**：Kioku Lite 的 tri-hybrid + RRF 思想可吸收，但用标准轻量组件（Chroma+FastEmbed+RRF）实现更干净，不引入 agent 记忆引擎。

---

## 四、优先级判断与理由

1. **阶段1 先行**：低风险、高收益、独立模块，快速给产品提精准度，且阶段2 重构时检索层可顺势成为 handler 底层，不产生返工。
2. **阶段2 紧随**：团长点名「路由层出大问题」，是结构性负债；但因其风险中、需设计评审，不在阶段1 前硬上，避免两头同时动主干导致回归失控。
3. **阶段3 不可省**：两条线必须在此汇合验证，否则「检索准了但路由没接到」或「路由分了但检索还是老的」都白做。
4. **阶段4 冻结**：未达触发条件前坚决不上，守住轻量。

---

## 五、关于 kioku-nest（澄清与处置）

- 用户提到的 `kioku-nest` 经检索无此包；高度吻合描述的是 **Kioku Lite（kioku-agent-kit-lite）**，确含向量+知识图谱且轻量（纯 SQLite、无 Docker、无 API key）。
- **但它是给 AI agent 做长期记忆的引擎，非产品文档检索库**；集成需进程间调用，且图谱对当前问答属过度设计。
- **处置**：不引入 Kioku Lite；吸收其「tri-hybrid + RRF 融合」思想，用 Chroma+FastEmbed+RRF 标准轻量组合实现（见阶段1）。

---

## 六、引用标注（防爆雷）

- Chroma 嵌入式向量库：开源 `chromadb`（trychroma.com）
- FastEmbed 本地 embedding（bge-small-zh）：Qdrant `fastembed`（github.com/qdrant/fastembed）
- RRF（Reciprocal Rank Fusion）：标准信息检索融合方法，Cormack et al., 2009
- Kioku Lite 定位 / tri-hybrid / 知识图谱记忆引擎：phuc-nt, *Kioku: Why I built a Knowledge Graph memory engine for my AI agents*, DEV Community（dev.to/phucnt/...）
- 路由分层（意图识别 + handler 分治 + 兜底）：标准 agent/对话系统架构模式，非特定厂商专利

---

## 七、给下一步的交接

- 阶段1 可立即开工：需 `build_index.py` 输入素材 A/B/C 路径 + Chroma 持久化目录约定。
- 阶段2 开工前需先出「意图清单 + handler 映射表」设计评审稿（建议作为独立一页纸，与阶段1 并行起草）。
- 全部以「新构造验收用例」为回归准绳，历史测试数据不进判断链。
