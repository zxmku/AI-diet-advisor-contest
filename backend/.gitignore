# 运行时数据库产物（启动时由 init_db 自动重建，不纳入交付包）
*.db
*.db-shm
*.db-wal

# 密钥与本地配置
.env

# 项目内密钥箱：密文与私钥绝不进版本库（vault.pub 公钥可安全提交）
*.vsec
local.key

# Python 缓存
__pycache__/
*.pyc

# M17 成本闸门运行时数据（账本/回复缓存，不入库）
# 注意：本文件位于 backend/ 下，/data/ 锚定 backend/data/ 目录。
/data/
