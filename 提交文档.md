# HealthPick · AI 智能膳食顾问 — 提交文档

## 这是什么

HealthPick 是我带队做的 AI 膳食陪伴顾问，给减脂、增肌、控糖这几类人用。它不只是个查热量的工具——能搭出完整一餐（食材+克数+热量+做法+替换），记得住你的目标和过敏，你加班嘴馋的时候它先共情、再给个不痛苦的替代。聊得起来、记得住我、陪你坚持，这是产品定义里写死的东西，不是套模板。

## 我拍板的几个设计

**架构是我定的。** 后端用 FastAPI 单端口同时托管 API 和前端页面，前端零构建零依赖，丢上去就能开。知识库拆成 A（营养基础）、B（三套方案）、C（平台服务）三块，其中 C 库做物理隔离，永远不会混进营养问答里——这条是评审硬规则，我在代码层就焊死了，不是靠配置开关。对话走"硬规则引擎 + 检索 grounding + 大模型润色"这条管线，大模型不可用的时候自动降级到本地规则，绝不让用户对着白屏。

**人机交互（我们内部叫"人设"）也是我设计的。** 先共情后建议、记得用户、陪着坚持，这些交互契约是产品层定好的，不是 AI 自己发挥。具体交互长什么样、边界在哪，写在《使用说明》里。

## 这次提交带的东西

| 文件 | 内容 |
|---|---|
| 架构说明.md | 系统架构、知识库挂载方式、推荐逻辑、多轮对话、异常处理、平台选择理由 |
| 使用说明.md | 怎么访问、怎么用、能力边界、合规约定（含我设计的人设交互） |
| 基础功能测试记录.md | 端点 / 端口 / 模型 / 营养表等机器功能的逐条核验 |
| AI辅助标注.md | AI 辅助声明，以及人工审核里发现并清理掉的真问题 |
| 复盘/硬编码后遗症复盘.md | 一次从状态图编排到硬编码的真实踩坑记录 |

## AI 辅助标注（看一眼）

这个项目**每一行代码都是 AI 写的**，我（团长）负责架构与交互设计、做最终审核。人工审核过程中抓到并已处理的问题：数值护栏一度被"约/左右"这类对冲词击穿、测试会话污染导致 6 条假失败、配置漂移造成一批过期断言——这些都在交付前清掉了，细节记在《AI辅助标注》里。

## 本地怎么跑

见《本地部署方法.md》：Docker 一行 `docker compose up -d` 起在 8137 端口，或者 Windows 本机离线双击 `deploy/一键启动.bat` 就行。

## 几条红线

拒药硬拦截、禁忌全链路排除、医疗免责、C 库隔离、数值只引素材原文——这些是评审的硬规则，代码层做了隔离，还有一道守门员审查清单对着逐条过。
